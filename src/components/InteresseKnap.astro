---
const { label } = Astro.props;
---

<details class="interesse-knap">
  <summary>{label}</summary>
</details>

<style>
  .interesse-knap {
    border: 2px solid #893f5f;
    padding: 5px 10px;
    border-radius: 8px;
    cursor: pointer;
    font-size: 15px;
    transition:
      background-color 0.3s ease,
      color 0.3s ease,
      border-color 0.3s ease;
  }

  /* Hover */
  .interesse-knap:hover {
    box-shadow: inset 0 0 0 2px #893f5f;
  }

  /* Klik */
  .interesse-knap[open] {
    background-color: #893f5f;
    color: white;
  }

  /* Fjern standard pil fra <summary> */
  .interesse-knap summary {
    list-style: none;
  }
</style>
